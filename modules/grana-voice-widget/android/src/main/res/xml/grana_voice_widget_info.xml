<?xml version="1.0" encoding="utf-8"?>
<!-- `updatePeriodMillis="0"`: o widget nunca se atualiza sozinho no relógio —
     ele só muda quando a pessoa toca ou quando o serviço/JS termina e chama
     a atualização. Qualquer período aqui seria bateria gasta pra redesenhar
     um botão parado.

     `targetCellWidth/Height` (Android 12+) é o que garante o 1x1 pedido;
     `minWidth/minHeight` continuam pro Android 11 e anteriores. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/grana_voice_widget"
    android:previewLayout="@layout/grana_voice_widget"
    android:description="@string/grana_voice_widget_descricao"
    android:resizeMode="none"
    android:widgetCategory="home_screen" />
